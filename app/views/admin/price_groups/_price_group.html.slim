tr
  td #{price_group.title}
  td #{price_group.customer_price}
  td #{price_group.contractor_price}
  td
    = link_to edit_admin_price_group_path(price_group) do
      i.far.fa-edit
    = link_to admin_price_group_path(price_group), method: :delete, data: { confirm: 'Вы уверены?' }, remote: true do
      i.far.fa-trash-alt

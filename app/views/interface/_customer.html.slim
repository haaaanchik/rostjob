header
  nav.navbar.fixed-top.navbar-dark.mdb-color.darken-4
    .container-fluid
      = render partial: 'interface/left_menu/customer_md', locals: { active_item: @active_item }
      = link_to root_path, class: 'navbar-brand' do
        = image_tag('/img/galka.png', height: '30px')
        - if Rails.env.staging?
          span.counter.red
            b stage
      = render partial: 'interface/navbar'
  #home
main
  #modal_container
  .container-fluid#right_window
    .row
      = render partial: 'interface/left_menu/customer', locals: { active_item: @active_item }
      = yield
